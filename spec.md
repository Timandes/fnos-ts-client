# 长时间连接断连问题修复规范

## 背景

用户反馈 fnos-ts-client 在保持连接超过 10 秒后会出现频繁断连的问题。需要调查原因并提供修复方案。

## 需求描述

### 功能需求

1. **连接稳定性需求**
   - 客户端应能够保持长时间的 WebSocket 连接（至少 120 秒）
   - 在连接期间不应出现无原因的断连
   - 心跳机制应正确工作，保持连接活跃

2. **状态管理需求**
   - `isConnected()` 方法应准确反映连接的实际状态
   - 连接断开时应及时更新内部状态
   - 不应出现状态与实际连接不一致的情况

3. **错误处理需求**
   - 应正确处理连接超时、网络错误等异常情况
   - 异常情况下应正确清理资源
   - 应提供足够的日志信息帮助诊断问题

### 非功能需求

1. **性能需求**
   - 心跳机制不应过度消耗资源
   - 状态检查应高效完成

2. **兼容性需求**
   - 修复方案应保持与现有 API 的兼容性
   - 应参考 Python 版本 (pyfnos) 的实现，保持行为一致

3. **可维护性需求**
   - 代码应易于理解和维护
   - 应添加必要的注释说明关键逻辑

## 接受标准

### 连接稳定性测试

**WHEN** 客户端连接到服务器并保持连接至少 120 秒
**THEN** 客户端不应出现无原因的断连（断连次数为 0）

**WHEN** 客户端在连接期间定期执行操作（如获取用户信息）
**THEN** 所有操作应成功完成，不应出现 "NotConnectedError"

### 状态准确性测试

**WHEN** WebSocket 连接实际断开
**THEN** `isConnected()` 方法应立即返回 `false`

**WHEN** WebSocket 连接成功建立
**THEN** `isConnected()` 方法应返回 `true`

### 心跳机制测试

**WHEN** 客户端保持连接
**THEN** 应每 30 秒发送一次心跳请求

**WHEN** 心跳响应超时
**THEN** 应正确处理超时情况并记录日志

### 错误处理测试

**WHEN** 连接超时
**THEN** 应清理所有资源，包括 WebSocket 引用、心跳定时器等

**WHEN** 发生网络错误
**THEN** 应正确处理错误并更新连接状态

## 边界情况

1. **网络波动**
   - 在网络波动情况下，客户端应尝试保持连接
   - 不应频繁触发重连

2. **服务器重启**
   - 当服务器重启时，客户端应检测到连接断开
   - `isConnected()` 应返回 `false`

3. **并发操作**
   - 在多个操作同时进行时，状态管理应保持一致性

## 依赖项

1. **参考实现**
   - pyfnos (Python 版本): https://github.com/Timandes/pyfnos
   - 特别是心跳机制和连接状态管理的实现

2. **WebSocket 库**
   - ws: 当前使用的 WebSocket 库
   - 需要正确处理连接状态和错误

## 约束条件

1. **技术约束**
   - 使用 TypeScript
   - 使用 ws 库进行 WebSocket 通信
   - 保持与现有 API 的兼容性

2. **时间约束**
   - 心跳间隔固定为 30 秒（与服务器端一致）
   - 连接超时默认为 3 秒

## 相关文件

- `src/client.ts` - 主要客户端实现
- `examples/test_long_connection.ts` - 长时间连接测试程序
- `examples/reconnect.ts` - 重连功能示例
- `examples/manual_reconnect.ts` - 手动重连示例

## 验证结果

### 测试环境

- 服务器：本地 WebSocket 服务器（127.0.0.1:5666）
- 测试时长：120 秒
- 心跳间隔：30 秒

### 验证结果

**修复前：**
- 连接在 3 秒后被客户端主动关闭
- 服务端日志显示：`< CLOSE 1005`（客户端发起的关闭）
- 心跳包正常发送，但连接仍然被关闭

**修复后：**
- ✅ 连接保持稳定，未在 3 秒后关闭
- ✅ 心跳包正常发送和接收
- ✅ 连接可以保持 120 秒或更长时间

### 实际发现的问题

通过服务端日志分析，确认了问题的根本原因：

1. **服务端日志显示关闭状态码 `1005` 是从客户端发起的**
   ```
   DEBUG: < CLOSE 1005 (no status received [internal]) [0 bytes]
   ```
   `<` 表示从客户端接收到的关闭请求

2. **客户端在连接成功后没有清除 3 秒超时定时器**
   - 定时器在 3 秒后触发
   - 主动调用 `this.ws.close()` 关闭连接

3. **心跳机制工作正常**
   - 服务端日志显示收到了 `{"req":"ping"}` 消息
   - 客户端日志显示收到了 `pong` 响应

## 版本历史

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|----------|
| 1.0 | 2025-01-30 | iFlow CLI | 初始版本 |